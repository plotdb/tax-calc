#!/usr/bin/env node
// Generated by LiveScript 1.6.0
var fs, path, yargs, lib, tax, argv, price, total, ret;
fs = require('fs');
path = require('path');
yargs = require('yargs');
lib = path.dirname(fs.realpathSync(__filename));
console.log(lib);
tax = require(lib + "/tax");
argv = yargs.option('price', {
  alias: 'p',
  description: "price ( untaxed )",
  type: 'number'
}).option('total', {
  alias: 't',
  description: "total amount (tax included)",
  type: 'number'
}).help('help').alias('help', 'h').check(function(argv, options){
  if (!(argv.price != null || argv.total)) {
    return new Error("need either price or total amount");
  }
  return true;
}).argv;
price = argv.price;
total = argv.total;
ret = tax.from(price
  ? {
    price: price
  }
  : {
    total: total
  });
console.log("未  稅: " + ret.price + " 元");
console.log("營業稅: " + ret.tax + " 元");
console.log("含  稅: " + ret.total + " 元");
